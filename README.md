# Como usar


## Automaticamente

- `npm install`
- `npm start`
- `GET localhost:9876/start` para registrar hora de entrada
- `GET localhost:9876/end` para registrar hora de saída
- `npm run fill` para preencher o Oris

## Manualmente

- edite o arquivo `apontamento.json` com as datas e horas seguindo o exemplo
- altere as linhas 16 e 17 com seu cpf e senha
- `npm install`
- `npm run fill` para preencher o Oris